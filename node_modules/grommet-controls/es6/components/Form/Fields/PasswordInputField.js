import React from 'react';
import { PasswordInput } from '../../PasswordInput';
import withField from '../hocs';

export default withField(function (props) {
  return React.createElement(PasswordInput, props);
});